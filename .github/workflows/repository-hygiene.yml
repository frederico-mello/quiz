name: Repository Hygiene Audit

on:
  schedule:
    - cron: "0 6 * * 1"
  push:
    paths:
      - auditoria.yaml
      - .github/**
      - .opencode/**
      - openspec/**
      - docs/**
      - README.md
      - .gitignore
      - Makefile
  pull_request:
    paths:
      - auditoria.yaml
      - .github/**
      - .opencode/**
      - openspec/**
      - docs/**
      - README.md
      - .gitignore
      - Makefile
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: read

jobs:
  audit:
    uses: frederico-mello/repository-hygiene/.github/workflows/audit-reusable.yml@0b3a3684a961554836672054b3576361e3d4116d
    with:
      python-version: "3.10"
